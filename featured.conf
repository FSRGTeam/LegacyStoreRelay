# Кто стоит на баннерах и в каком порядке. Одна строка — один bundleId.
#
# Это единственное место, где решает модератор. Что будет написано на баннере,
# решает автор: цитату и подпись он присылает вместе с приложением, они лежат в
# его карточке и попадают в каталог. Здесь — только очередь.
#
# Строка, для которой нет приложения в каталоге, молча пропускается при сборке,
# поэтому сюда можно вписывать заранее.

com.example.LegacyGram
com.zeptolab.candyflick

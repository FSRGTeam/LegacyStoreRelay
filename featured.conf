# Кто стоит на баннерах и в каком порядке. Одна строка — один bundleId.
#
# Правит этот список сайт: панель, раздел «Баннеры», доступный админу и выше.
# Что будет написано на баннере, решает тот же человек — цитата и подпись
# лежат в карточке приложения в шарде, и туда их пишет админ, а не автор.
# Раньше их присылал автор вместе со сборкой, и витрина говорила его словами
# о нём же.
#
# Строка, для которой нет приложения в каталоге, молча пропускается при сборке,
# поэтому сюда можно вписывать заранее.
